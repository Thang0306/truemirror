{
  "$schema": "https://railway.app/railway.schema.json",
  "build": {
    "builder": "NIXPACKS"
  },
  "deploy": {
    "startCommand": "cd backend && gunicorn app:socketio --bind 0.0.0.0:$PORT --workers 1 --timeout 120 --worker-class sync"
  }
}